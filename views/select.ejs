<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>モンスター一覧</h2>
  <table border="1">
    <tr>
      <th>ID </th>
      <th>モンスター名　</th>
    </tr>
      <% for( let row of data ) { %>
        <tr>
            <td><a href="/monster/<%=row.id%>"><%= row.id%></a></td>
            <td><%= row.name %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>